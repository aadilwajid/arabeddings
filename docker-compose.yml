version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=ara-beddings-secret-key-2026
    volumes:
      - ./data:/app/data
    restart: unless-stopped
